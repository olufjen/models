class Scheduler
!!!483458.java!!!	Scheduler(in k : int, in lam : double, in limit : int)
		this.k = k;
		this.lam = lam;
		this.limit = limit;
!!!483586.java!!!	Scheduler()
		this.k = 20;
		this.lam = 0.045;
		this.limit = 100;
!!!483714.java!!!	getTemp(in t : int) : double
		if (t < limit)
			return k * Math.exp((-1) * lam * t);
		else
			return 0.0;
