class AbstractModulation
!!!280578.java!!!	getMatchingSubstitution(inout toMatch : Term, inout expression : AtomicSentence) : AbstractModulation::IdentifyCandidateMatchingTerm

		IdentifyCandidateMatchingTerm icm = new IdentifyCandidateMatchingTerm(
				toMatch, expression);

		if (icm.isMatch()) {
			return icm;
		}

		// indicates no match
		return null;
