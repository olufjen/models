class ChessHighLevelAction
!!!1652610.java!!!	ChessHighLevelAction(in name : String, inout variables : List<Term>, in precondition : String, in effects : String, inout refinements : List<List<ActionSchema>>)
        super(name, variables, precondition, effects);
        this.refinements = refinements;
!!!1652738.java!!!	addRefinement(inout newRefinement : List<ActionSchema>) : void
        this.refinements.add(newRefinement);
!!!1652866.java!!!	getRefinements() : List<List<ActionSchema>>
        return refinements;
!!!1652994.java!!!	toString() : String
        String result = super.toString();
        result = result + "\n" + "REFINEMENTS : \n";
        for (List<ActionSchema> refinement :
                this.getRefinements()) {
            result += "\n";
            for (ActionSchema action :
                    refinement) {
                result = result + "\n" + (action.getName());
            }
        }
        return result;
