class DistanceStepCostFunction
!!!169474.java!!!	DistanceStepCostFunction(inout map : Map)
            this.map = map;
!!!169602.java!!!	applyAsDouble(in state : String, inout action : MoveToAction, in statePrimed : String) : double
            Double distance = map.getDistance(state, statePrimed);
            if (distance == null || distance <= 0)
                return constantCost;
            return distance;
