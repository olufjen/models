class QueensEnvironment
!!!2540674.java!!!	QueensEnvironment(inout board : NQueensBoard)
		super();
		this.board = board;
!!!2540802.java!!!	executeAction(inout agent : Agent, inout action : Action) : void
		if (action instanceof QueenAction) {
			QueenAction act = (QueenAction) action;
			XYLocation loc = new XYLocation(act.getX(), act.getY());
			if (act.getName() == QueenAction.PLACE_QUEEN)
				board.addQueenAt(loc);
			else if (act.getName() == QueenAction.REMOVE_QUEEN)
				board.removeQueenFrom(loc);
			else if (act.getName() == QueenAction.MOVE_QUEEN)
				board.moveQueenTo(loc);
		}

!!!2540930.java!!!	getPerceptSeenBy(inout anAgent : Agent) : Percept
		// TODO Auto-generated method stub
		
		return null;
!!!2541058.java!!!	getBoard() : NQueensBoard
		return board;
!!!2541186.java!!!	setBoard(inout board : NQueensBoard) : void
		this.board = board;
