class ActionStore
!!!669826.java!!!	add(inout action : ChessAction, in utilValue : double) : void
            int idx = 0;
            while (idx < actions.size() && utilValue <= utilValues.get(idx))
                idx++;
            actions.add(idx, action);
            utilValues.add(idx, utilValue);
!!!669954.java!!!	size() : int
            return actions.size();
