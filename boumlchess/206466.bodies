class IntegrableApplication
!!!552322.java!!!	start(inout primaryStage : Stage) : void
		primaryStage.setTitle(getTitle());
		primaryStage.setScene(new Scene(createRootPane(), sceneWidth, sceneHeight));
		primaryStage.show();
		initialize();
