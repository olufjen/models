class ActionStore
!!!149122.java!!!	add(inout action : A, in utilValue : double) : void
            int idx = 0;
            while (idx < actions.size() && utilValue <= utilValues.get(idx))
                idx++;
            actions.add(idx, action);
            utilValues.add(idx, utilValue);
!!!149250.java!!!	size() : int
            return actions.size();
