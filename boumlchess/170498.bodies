class HighLevelAction
!!!357634.java!!!	HighLevelAction(in name : String, inout variables : List<Term>, in precondition : String, in effects : String, inout refinements : List<List<ActionSchema>>)
        super(name, variables, precondition, effects);
        this.refinements = refinements;
!!!357762.java!!!	addRefinement(inout newRefinement : List<ActionSchema>) : void
        this.refinements.add(newRefinement);
!!!357890.java!!!	getRefinements() : List<List<ActionSchema>>
        return refinements;
!!!358018.java!!!	toString() : String
        String result = super.toString();
        result = result + "\n" + "REFINEMENTS : \n";
        for (List<ActionSchema> refinement :
                this.getRefinements()) {
            result += "\n";
            for (ActionSchema action :
                    refinement) {
                result = result + "\n" + (action.getName());
            }
        }
        return result;
